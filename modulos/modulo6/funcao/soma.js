function soma(n1 = 0, n2 = 0) {
  // Parâmetros n1 e n2 com valores padrão 0
  // Função que soma dois números
  return n1 + n2;
}

console.log(soma(5)); // Chama a função soma com um argumento, n2 assume o valor padrão 0
