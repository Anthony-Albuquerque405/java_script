let num = [5, 8, 2, 9, 3]; // Declaração do array

for (let pos in num) {
  // Laço que percorre o array
  console.log(`A posição ${pos} tem o valor ${num[pos]}`); // Mostra a posição e o valor do índice
}
